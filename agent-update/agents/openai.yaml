interface:
  display_name: "Agent Update"
  short_description: "Sync guidance and skills into configured locations"
  default_prompt: "Use $agent-update to refresh shared agent guidance, canonical skills, and the configured entry-point links."
