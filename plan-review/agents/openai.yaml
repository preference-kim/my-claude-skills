interface:
  display_name: "Plan Review"
  short_description: "Independent read-only review of an existing plan"
  default_prompt: "Use $plan-review to independently review this existing plan."
policy:
  allow_implicit_invocation: true
